# --- Telegram Bot Config ---
BOT_TOKEN=your_telegram_bot_token
OWNER_ID=your_telegram_user_id

# --- API & Security ---
# Secret for JWT signing
JWT_SECRET=generate_a_long_random_string_here
# Token for internal bot-to-api communication (must match)
BOT_SERVICE_TOKEN=generate_another_random_string

# --- Deployment URLs ---
# This is the public URL where your Web App will be hosted (e.g., https://dave.sports)
WEBAPP_URL=https://your-domain.com
# Port for the FastAPI server (default 8000)
WEBAPP_PORT=8000
# Internal API URL for bot-to-API communication (use 127.0.0.1 when running in same container)
API_BASE_URL=http://127.0.0.1:8000
# Database URL (for SQLite use: sqlite:///./dave_sports.db)
DATABASE_URL=sqlite:///./dave_sports.db
